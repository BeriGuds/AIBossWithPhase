using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InstantiateScript : MonoBehaviour
{
    //public GameObject Enem
    //// Start is called before the first frame update
    //void Start()
    //{
    //    // Instantiate the prefab and store the instance in a variable
    //    GameObject instantiatedObject = Instantiate(prefabToInstantiate);

    //    // Access the script attached to the instantiated object
    //    EnemyBoss scriptComponent = instantiatedObject.GetComponent<EnemyBoss>();
    //}
}
